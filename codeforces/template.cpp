#include <iostream>
#include <fstream>
#include <algorithm>
using namespace std;

int main() {
  /* COMMMENT BEFORE SUBMITTING */
  ifstream cin; cin.open("data.txt");
  /* COMMMENT BEFORE SUBMITTING */
  ios_base::sync_with_stdio(false);
  cin.tie(NULL);

  int a, b;
  cin >> a;
  cout << a+b << "\n";

  return 0;
}
